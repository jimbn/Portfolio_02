{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "",
  "engines": {
    "node": "18.16"
  },
  "scripts": {
    "start": "cd react-portfolio && npm run start",
    "develop": "cd react-portfolio && npm start",
    "install": "cd react-portfolio && npm i",
    "build": "cd react-portfolio && npm run build",
    "predeploy": "cd react-portfolio && npm run build",
    "deploy": "cd react-portfolio && gh-pages -d build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gh-pages": "^5.0.0"
  }
}
